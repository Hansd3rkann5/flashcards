HOST=0.0.0.0
PORT=8000
# Optional: absolute path to repository root (where index.html lives).
# FLASHCARDS_APP_ROOT=/Users/you/coding_local/flashcards
# Optional: absolute path to frontend web root (e.g. Vite build output).
# Defaults to ./frontend/dist when available, otherwise FLASHCARDS_APP_ROOT.
# FLASHCARDS_WEB_ROOT=/Users/you/coding_local/flashcards/frontend/dist
# Optional: absolute path to sqlite file.
# FLASHCARDS_DB_PATH=/Users/you/coding_local/flashcards/flashcards.sqlite3
